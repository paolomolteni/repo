keycloak.auth-server-url=http://localhost:8100/auth
keycloak.realm=spring-app
keycloak.resource=login-app
#keycloak.public-client=true

keycloak.credentials.secret=8dee8205-b4e3-40ef-ba62-e37b9ddc7bae

# Use Keycloak directly
#keycloak.security-constraints[0].authRoles[0]=user
#keycloak.security-constraints[0].securityCollections[0].patterns[0]=/internal/*

# Enable the client roles instead of the global roles
keycloak.use-resource-role-mappings=true

server.port=8081

# Used for Spring security - Keycloak integration
keycloak.principal-attribute=preferred_username

#fix Spring security bug
spring.main.allow-bean-definition-overriding=true